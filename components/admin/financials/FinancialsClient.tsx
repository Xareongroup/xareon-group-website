"use client";

import Link from "next/link";
import { useCallback, useEffect, useMemo, useState } from "react";
import { createClient } from "@/lib/supabase/client";
import type { Tables } from "@/lib/supabase/database.types";
import ConfirmDialog from "@/components/ui/ConfirmDialog";
import type { ExpenseFormValues } from "@/types/financial";

type View = "dashboard" | "expenses" | "categories" | "vendors" | "reports" | "expense-form" | "expense-detail";
type Expense = Tables<"expenses"> & { category?: Pick<Tables<"expense_categories">, "name" | "group_name"> | null; vendor?: Pick<Tables<"vendors">, "name"> | null; job?: Pick<Tables<"jobs">, "job_number" | "title"> | null };
type Category = Tables<"expense_categories"> & { company?: string | null; email?: string | null; phone?: string | null };
type Vendor = Tables<"vendors"> & { group_name?: string };
type Invoice = Pick<Tables<"invoices">, "id" | "total" | "amount_paid" | "balance_due" | "status" | "issue_date" | "job_id">;
type Job = Pick<Tables<"jobs">, "id" | "job_number" | "title" | "status" | "scheduled_date" | "completed_date">;
const currency = (value: number) => new Intl.NumberFormat("en-US", { style: "currency", currency: "USD" }).format(value || 0);
const initialExpense: ExpenseFormValues = { date: new Date().toISOString().slice(0, 10), category_id: "", vendor_id: "", customer_id: "", job_id: "", employee_id: "", description: "", amount: "", payment_method: "Cash", status: "Paid", receipt_url: "", notes: "" };

export default function FinancialsClient({ view, expenseId }: { view: View; expenseId?: string }) {
  const supabase = useMemo(() => createClient(), []);
  const [expenses, setExpenses] = useState<Expense[]>([]); const [categories, setCategories] = useState<Category[]>([]); const [vendors, setVendors] = useState<Vendor[]>([]); const [jobs, setJobs] = useState<Job[]>([]); const [invoices, setInvoices] = useState<Invoice[]>([]);
  const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const [query, setQuery] = useState(""); const [form, setForm] = useState(initialExpense); const [saving, setSaving] = useState(false); const [period, setPeriod] = useState("monthly");
  const [categoryForm, setCategoryForm] = useState({ name: "", group_name: "Other" }); const [vendorForm, setVendorForm] = useState({ name: "", company: "", email: "", phone: "", category: "", notes: "" });
  const [editingCategory, setEditingCategory] = useState<Category | null>(null); const [deleting, setDeleting] = useState<{ type: "expense" | "vendor" | "category"; id: string; name: string } | null>(null); const [deleteMessage, setDeleteMessage] = useState("");

  const load = useCallback(async () => {
    setLoading(true); setError("");
    const [expenseResult, categoryResult, vendorResult, jobResult, invoiceResult] = await Promise.all([
      supabase.from("expenses").select("*, category:expense_categories(name,group_name), vendor:vendors(name), job:jobs(job_number,title)").order("date", { ascending: false }),
      supabase.from("expense_categories").select("*").order("group_name").order("name"), supabase.from("vendors").select("*").order("name"),
      supabase.from("jobs").select("id,job_number,title,status,scheduled_date,completed_date").order("created_at", { ascending: false }),
      supabase.from("invoices").select("id,total,amount_paid,balance_due,status,issue_date,job_id"),
    ]);
    const firstError = [expenseResult.error, categoryResult.error, vendorResult.error, jobResult.error, invoiceResult.error].find(Boolean);
    if (firstError) setError(firstError.message);
    setExpenses((expenseResult.data ?? []) as Expense[]); setCategories(categoryResult.data ?? []); setVendors(vendorResult.data ?? []); setJobs(jobResult.data ?? []); setInvoices(invoiceResult.data ?? []); setLoading(false);
  }, [supabase]);
  useEffect(() => { void load(); }, [load]);
  useEffect(() => { const item = expenses.find((expense) => expense.id === expenseId); if (item) setForm({ date: item.date, category_id: item.category_id ?? "", vendor_id: item.vendor_id ?? "", customer_id: item.customer_id ?? "", job_id: item.job_id ?? "", employee_id: item.employee_id ?? "", description: item.description, amount: String(item.amount), payment_method: item.payment_method as ExpenseFormValues["payment_method"], status: item.status as ExpenseFormValues["status"], receipt_url: item.receipt_url ?? "", notes: item.notes ?? "" }); }, [expenseId, expenses]);

  async function saveExpense(event: React.FormEvent) { event.preventDefault(); setSaving(true); setError(""); const payload = { date: form.date, category_id: form.category_id || null, vendor_id: form.vendor_id || null, customer_id: form.customer_id || null, job_id: form.job_id || null, employee_id: form.employee_id || null, description: form.description, amount: Number(form.amount), payment_method: form.payment_method, status: form.status, receipt_url: form.receipt_url || null, notes: form.notes || null }; const result = expenseId ? await supabase.from("expenses").update(payload).eq("id", expenseId) : await supabase.from("expenses").insert({ ...payload, expense_number: `EXP-${Date.now()}` }); setSaving(false); if (result.error) return setError(result.error.message); window.location.assign("/admin/financials/expenses"); }
  async function saveCategory(event: React.FormEvent) { event.preventDefault(); const value = editingCategory ? await supabase.from("expense_categories").update(categoryForm).eq("id", editingCategory.id) : await supabase.from("expense_categories").insert(categoryForm); if (value.error) setError(value.error.message); else { setCategoryForm({ name: "", group_name: "Other" }); setEditingCategory(null); void load(); } }
  async function saveVendor(event: React.FormEvent) { event.preventDefault(); const result = await supabase.from("vendors").insert({ ...vendorForm, company: vendorForm.company || null, email: vendorForm.email || null, phone: vendorForm.phone || null, category: vendorForm.category || null, notes: vendorForm.notes || null }); if (result.error) setError(result.error.message); else { setVendorForm({ name: "", company: "", email: "", phone: "", category: "", notes: "" }); void load(); } }
  async function requestDelete(type: "expense" | "vendor" | "category", id: string, name: string) { if (type === "category") { const { count, error: countError } = await supabase.from("expenses").select("id", { count: "exact", head: true }).eq("category_id", id); if (countError) return setError(countError.message); if (count) { setDeleteMessage("This category is currently assigned to expenses. Reassign expenses before deleting."); return; } } setDeleting({ type, id, name }); }
  async function confirmDelete() { if (!deleting) return; setSaving(true); const table = deleting.type === "expense" ? "expenses" : deleting.type === "vendor" ? "vendors" : "expense_categories"; const { error: deleteError } = await supabase.from(table).delete().eq("id", deleting.id); setSaving(false); if (deleteError) setError(deleteError.message); else { setDeleting(null); void load(); } }

  const filteredExpenses = expenses.filter((item) => `${item.expense_number} ${item.description} ${item.vendor?.name ?? ""}`.toLowerCase().includes(query.toLowerCase()));
  const dateStart = useMemo(() => { const now = new Date(); if (period === "today") return now.toISOString().slice(0, 10); if (period === "weekly") { now.setDate(now.getDate() - 7); return now.toISOString().slice(0, 10); } if (period === "quarterly") { now.setMonth(now.getMonth() - 3); return now.toISOString().slice(0, 10); } if (period === "annually") { now.setFullYear(now.getFullYear() - 1); return now.toISOString().slice(0, 10); } if (period === "ytd") return `${now.getFullYear()}-01-01`; now.setMonth(now.getMonth() - 1); return now.toISOString().slice(0, 10); }, [period]);
  const periodExpenses = expenses.filter((item) => item.date >= dateStart); const periodInvoices = invoices.filter((item) => !item.issue_date || item.issue_date >= dateStart); const revenue = periodInvoices.reduce((sum, item) => sum + Number(item.total ?? 0), 0); const paidRevenue = periodInvoices.reduce((sum, item) => sum + Number(item.amount_paid ?? 0), 0); const outstanding = periodInvoices.reduce((sum, item) => sum + Number(item.balance_due ?? 0), 0); const totalExpenses = periodExpenses.reduce((sum, item) => sum + Number(item.amount), 0); const profit = revenue - totalExpenses;

  if (loading) return <div className="p-8 text-slate-500">Loading financial data…</div>;
  if (view === "expense-form") return <ExpenseForm form={form} setForm={setForm} categories={categories} vendors={vendors} jobs={jobs} saving={saving} error={error} onSave={saveExpense} />;
  if (view === "expense-detail") { const item = expenses.find((expense) => expense.id === expenseId); if (!item) return <div className="p-8">Expense not found.</div>; return <ExpenseDetail item={item} onDelete={() => void requestDelete("expense", item.id, item.expense_number)} />; }
  if (view === "categories" || view === "vendors") { const isCategory = view === "categories"; return <div className="mx-auto max-w-7xl space-y-6 px-6 py-8"><h1 className="text-3xl font-bold">{isCategory ? "Expense Categories" : "Vendors / Payees"}</h1><div className="grid gap-6 lg:grid-cols-[360px_1fr]"><form onSubmit={isCategory ? saveCategory : saveVendor} className="space-y-3 rounded-2xl border bg-white p-6 shadow-sm">{isCategory ? <><Field label="Name" value={categoryForm.name} onChange={(name) => setCategoryForm({ ...categoryForm, name })}/><Field label="Group" value={categoryForm.group_name} onChange={(group_name) => setCategoryForm({ ...categoryForm, group_name })}/></> : <><Field label="Name" value={vendorForm.name} onChange={(name) => setVendorForm({ ...vendorForm, name })}/><Field label="Company" value={vendorForm.company} onChange={(company) => setVendorForm({ ...vendorForm, company })}/><Field label="Email" value={vendorForm.email} onChange={(email) => setVendorForm({ ...vendorForm, email })}/><Field label="Phone" value={vendorForm.phone} onChange={(phone) => setVendorForm({ ...vendorForm, phone })}/></>}<button className="rounded-xl bg-blue-600 px-4 py-2 font-medium text-white">{editingCategory ? "Save Category" : `Add ${isCategory ? "Category" : "Vendor"}`}</button></form><div className="overflow-hidden rounded-2xl border bg-white shadow-sm"><table className="min-w-full text-sm"><thead className="bg-slate-50 text-left text-slate-500"><tr><th className="p-4">Name</th><th>Group / Company</th><th>Contact</th><th>Actions</th></tr></thead><tbody>{(isCategory ? categories : vendors).map((row) => <tr className="border-t" key={row.id}><td className="p-4 font-medium">{isCategory ? row.name : <Link className="text-blue-700 hover:underline" href={`/admin/financials/vendors/${row.id}`}>{row.name}</Link>}</td><td>{isCategory ? row.group_name : row.company ?? "—"}</td><td>{isCategory ? "—" : row.email ?? row.phone ?? "—"}</td><td className="space-x-2">{isCategory ? <button onClick={() => { const category = row as Category; setEditingCategory(category); setCategoryForm({ name: category.name, group_name: category.group_name }); }} className="text-blue-700">Edit</button> : <Link className="text-blue-700" href={`/admin/financials/vendors/${row.id}/edit`}>Edit</Link>}<button onClick={() => void requestDelete(isCategory ? "category" : "vendor", row.id, row.name)} className="text-red-600">Delete</button></td></tr>)}</tbody></table></div></div>{error && <p className="text-red-600">{error}</p>}</div>; }
  if (view === "dashboard" || view === "reports") return <FinancialReport period={period} setPeriod={setPeriod} revenue={revenue} paidRevenue={paidRevenue} outstanding={outstanding} expenses={totalExpenses} profit={profit} invoices={periodInvoices} jobs={jobs} expenseRows={periodExpenses} />;
  return <div className="mx-auto max-w-7xl space-y-6 px-6 py-8"><div className="flex items-center justify-between"><div><h1 className="text-3xl font-bold">Expenses</h1><p className="text-slate-500">Track costs, receipts, vendors, and project spending.</p></div><Link href="/admin/financials/expenses/new" className="rounded-xl bg-blue-600 px-4 py-3 font-medium text-white">New Expense</Link></div><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search expenses" className="w-full max-w-md rounded-xl border p-3"/><div className="overflow-hidden rounded-2xl border bg-white shadow-sm"><table className="min-w-full text-sm"><thead className="bg-slate-50 text-left text-slate-500"><tr><th className="p-4">Expense</th><th>Date</th><th>Category</th><th>Vendor</th><th>Amount</th><th>Status</th><th>Actions</th></tr></thead><tbody>{filteredExpenses.map((item) => <tr key={item.id} className="border-t"><td className="p-4 font-medium">{item.expense_number}<div className="text-slate-500">{item.description}</div></td><td>{item.date}</td><td>{item.category?.name ?? "—"}</td><td>{item.vendor?.name ?? "—"}</td><td>{currency(Number(item.amount))}</td><td>{item.status}</td><td className="space-x-2"><Link className="text-blue-700" href={`/admin/financials/expenses/${item.id}`}>View</Link><Link className="text-blue-700" href={`/admin/financials/expenses/${item.id}/edit`}>Edit</Link><button onClick={() => void requestDelete("expense", item.id, item.expense_number)} className="text-red-600">Delete</button></td></tr>)}</tbody></table></div>{error && <p className="text-red-600">{error}</p>}<ConfirmDialog open={Boolean(deleting)} title={`Delete ${deleting?.type ?? "record"}`} description={deleting?.type === "vendor" ? "Are you sure you want to delete this vendor?" : `Are you sure you want to delete this ${deleting?.type ?? "record"}?`} confirmText="Delete" loading={saving} onConfirm={confirmDelete} onCancel={() => setDeleting(null)}/><ConfirmDialog open={Boolean(deleteMessage)} title="Category in use" description={deleteMessage} confirmText="OK" confirmVariant="primary" onConfirm={() => setDeleteMessage("")} onCancel={() => setDeleteMessage("")}/></div>;
}

function Field({ label, value, onChange }: { label: string; value: string; onChange: (value: string) => void }) { return <label className="block text-sm font-medium">{label}<input required={label === "Name"} value={value} onChange={(event) => onChange(event.target.value)} className="mt-2 w-full rounded-xl border p-3"/></label>; }
function ExpenseForm({ form, setForm, categories, vendors, jobs, saving, error, onSave }: { form: ExpenseFormValues; setForm: React.Dispatch<React.SetStateAction<ExpenseFormValues>>; categories: Category[]; vendors: Vendor[]; jobs: Job[]; saving: boolean; error: string; onSave: (event: React.FormEvent) => Promise<void> }) { const input = (label: string, key: keyof ExpenseFormValues, type = "text") => <label className="block text-sm font-medium">{label}<input required={key === "description" || key === "amount"} type={type} value={form[key]} onChange={(event) => setForm({ ...form, [key]: event.target.value })} className="mt-2 w-full rounded-xl border p-3"/></label>; return <form onSubmit={onSave} className="mx-auto max-w-4xl space-y-6 px-6 py-8"><h1 className="text-3xl font-bold">{form.description ? "Edit Expense" : "New Expense"}</h1><div className="grid gap-5 rounded-2xl border bg-white p-6 shadow-sm md:grid-cols-2">{input("Description", "description")}{input("Amount", "amount", "number")}{input("Date", "date", "date")}<label>Category<select value={form.category_id} onChange={(event) => setForm({ ...form, category_id: event.target.value })} className="mt-2 w-full rounded-xl border p-3"><option value="">None</option>{categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select></label><label>Vendor<select value={form.vendor_id} onChange={(event) => setForm({ ...form, vendor_id: event.target.value })} className="mt-2 w-full rounded-xl border p-3"><option value="">None</option>{vendors.map((vendor) => <option key={vendor.id} value={vendor.id}>{vendor.name}</option>)}</select></label><label>Job<select value={form.job_id} onChange={(event) => setForm({ ...form, job_id: event.target.value })} className="mt-2 w-full rounded-xl border p-3"><option value="">None</option>{jobs.map((job) => <option key={job.id} value={job.id}>{job.job_number ?? job.title ?? "Job"}</option>)}</select></label><label>Status<select value={form.status} onChange={(event) => setForm({ ...form, status: event.target.value as ExpenseFormValues["status"] })} className="mt-2 w-full rounded-xl border p-3">{["Paid", "Pending", "Reimbursed"].map((value) => <option key={value}>{value}</option>)}</select></label><label>Payment method<select value={form.payment_method} onChange={(event) => setForm({ ...form, payment_method: event.target.value as ExpenseFormValues["payment_method"] })} className="mt-2 w-full rounded-xl border p-3">{["Cash", "Credit Card", "Bank Transfer", "Check"].map((value) => <option key={value}>{value}</option>)}</select></label><label className="md:col-span-2">Notes<textarea value={form.notes} onChange={(event) => setForm({ ...form, notes: event.target.value })} className="mt-2 w-full rounded-xl border p-3" rows={4}/></label>{error && <p className="text-red-600 md:col-span-2">{error}</p>}<button disabled={saving} className="rounded-xl bg-blue-600 px-5 py-3 font-medium text-white">{saving ? "Saving…" : "Save Expense"}</button></div></form>; }
function ExpenseDetail({ item, onDelete }: { item: Expense; onDelete: () => void }) { return <div className="mx-auto max-w-4xl space-y-6 px-6 py-8"><div className="flex justify-between"><div><h1 className="text-3xl font-bold">{item.expense_number}</h1><p className="text-slate-500">{item.description}</p></div><div className="space-x-3"><Link className="rounded-xl bg-blue-600 px-4 py-2 text-white" href={`/admin/financials/expenses/${item.id}/edit`}>Edit</Link><button onClick={onDelete} className="rounded-xl border border-red-200 px-4 py-2 text-red-600">Delete</button></div></div><div className="grid gap-4 rounded-2xl border bg-white p-6 shadow-sm md:grid-cols-2"><p><b>Amount:</b> {currency(Number(item.amount))}</p><p><b>Date:</b> {item.date}</p><p><b>Vendor:</b> {item.vendor?.name ?? "—"}</p><p><b>Category:</b> {item.category?.name ?? "—"}</p><p><b>Status:</b> {item.status}</p><p><b>Receipt:</b> {item.receipt_url ? <a className="text-blue-700" href={item.receipt_url}>View attachment</a> : "—"}</p><p className="md:col-span-2"><b>Notes:</b> {item.notes ?? "—"}</p></div></div>; }
function FinancialReport({ period, setPeriod, revenue, paidRevenue, outstanding, expenses, profit, invoices, jobs, expenseRows }: { period: string; setPeriod: (value: string) => void; revenue: number; paidRevenue: number; outstanding: number; expenses: number; profit: number; invoices: Invoice[]; jobs: Job[]; expenseRows: Expense[] }) { const margin = revenue ? profit / revenue * 100 : 0; const completed = jobs.filter((job) => job.status === "Completed").length; const scheduled = jobs.filter((job) => job.status === "Scheduled").length; return <div className="mx-auto max-w-7xl space-y-6 px-6 py-8"><div className="flex items-center justify-between"><div><h1 className="text-3xl font-bold">Financial Reports</h1><p className="text-slate-500">Operational performance for the selected period.</p></div><select value={period} onChange={(event) => setPeriod(event.target.value)} className="rounded-xl border p-3">{[["today","Today"],["weekly","Weekly"],["monthly","Monthly"],["quarterly","Quarterly"],["annually","Annually"],["ytd","Year To Date"]].map(([value,label]) => <option key={value} value={value}>{label}</option>)}</select></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{[["Total Revenue",revenue],["Paid Revenue",paidRevenue],["Outstanding",outstanding],["Total Expenses",expenses],["Gross Profit",profit],["Profit Margin",margin]].map(([label,value]) => <div key={String(label)} className="rounded-2xl border bg-white p-5 shadow-sm"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-2xl font-bold">{String(label).includes("Margin") ? `${Number(value).toFixed(1)}%` : currency(Number(value))}</p></div>)}</div><div className="grid gap-6 md:grid-cols-2"><div className="rounded-2xl border bg-white p-6 shadow-sm"><h2 className="font-semibold">Invoices & Payments</h2><p className="mt-3">Paid: {invoices.filter((invoice) => invoice.status === "Paid").length} · Sent: {invoices.filter((invoice) => invoice.status === "Sent").length} · Draft: {invoices.filter((invoice) => invoice.status === "Draft").length} · Overdue: {invoices.filter((invoice) => invoice.status === "Overdue").length}</p></div><div className="rounded-2xl border bg-white p-6 shadow-sm"><h2 className="font-semibold">Jobs</h2><p className="mt-3">Total: {jobs.length} · Completed: {completed} · Scheduled: {scheduled} · Revenue per job: {currency(jobs.length ? revenue / jobs.length : 0)}</p></div><div className="rounded-2xl border bg-white p-6 shadow-sm md:col-span-2"><h2 className="font-semibold">Expense categories and vendor spending</h2>{expenseRows.length === 0 ? <p className="mt-3 text-slate-500">No expenses in this period.</p> : <ul className="mt-3 grid gap-2 md:grid-cols-2">{expenseRows.slice(0, 10).map((expense) => <li key={expense.id}>{expense.category?.name ?? "Uncategorized"} · {expense.vendor?.name ?? "No vendor"}: {currency(Number(expense.amount))}</li>)}</ul>}</div></div></div>; }
